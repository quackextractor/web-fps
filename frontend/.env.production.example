# Production Environment Configuration
# Copy this file to .env.production or set these variables in your hosting provider's environment settings.
# Do NOT commit the actual values to version control.

# Supabase (PostgreSQL) Connection String - Used for pooled connections
DATABASE_URL=""

# Supabase (PostgreSQL) Direct Connection String - Used for direct connections (e.g., migrations)
DIRECT_URL=""

# JSON Web Token Secret - Used for signing and verifying JWTs
JWT_SECRET=""
